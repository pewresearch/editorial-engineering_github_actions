name: Release PRC-Quiz-Builder
on:
  release:
    types: [published]

jobs:
  release-prc-chart-builder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Release PRC Quiz Builder To Repo
        id: push_directory
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.PRC_PLATFORM_GITHUB_BOT_TOKEN }}
        with:
          source-directory: 'plugins/prc-quiz-builder'
          destination-github-username: 'prcdevgitbot'
          destination-repository-username: 'pewresearch'
          destination-repository-name: 'prc-quiz-builder'
          user-email: 'webdev@pewresearch.org'
          commit-message: See ORIGIN_COMMIT from $GITHUB_REF
          target-branch: main
